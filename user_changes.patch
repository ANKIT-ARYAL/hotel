--- a/src/components/homepage/HeroSection.tsx
+++ b/src/components/homepage/HeroSection.tsx
@@ -27,15 +27,20 @@ export function HeroSection({ settings }: HeroSectionProps) {
          initial={{ scale: 1.1 }}
          animate={{ scale: 1 }}
          transition={{ duration: 1.5, ease: [0.16, 1, 0.3, 1] }}
          className="absolute inset-0 w-full h-full z-0"
        >
-        <video
-          autoPlay
-          loop
-          muted
-          playsInline
-          className="w-full h-full object-cover"
-          poster={settings.backgroundImageFallback}
-        >
-          <source src={settings.videoUrl} type="video/mp4" />
-        </video>
+        {settings.videoUrl ? (
+          <video
+            autoPlay
+            loop
+            muted
+            playsInline
+            className="w-full h-full object-cover"
+            poster={settings.backgroundImageFallback}
+          >
+            <source src={settings.videoUrl} type="video/mp4" />
+          </video>
+        ) : (
+          <div 
+            className="w-full h-full bg-cover bg-center"
+            style={{ backgroundImage: `url(${settings.image || settings.backgroundImageFallback})` }}
+          />
+        )}
        </motion.div>
        
        {/* Editorial Gradient Scrim (stronger at left and center) */}
@@ -52,14 +59,14 @@ export function HeroSection({ settings }: HeroSectionProps) {
        <div className="absolute inset-0 bg-gradient-to-r from-black/80 via-black/20 to-transparent z-10" />
  
        {/* Content locked to left-middle */}
-      <div className="relative z-20 w-full max-w-[1600px] mx-auto flex flex-col gap-12">
+      <div className="relative z-20 w-full mx-auto flex flex-col gap-12">
          
-        <div className="max-w-4xl flex-1">
+        <div className="w-full flex-1">
            <motion.h1 
              initial={{ opacity: 0, x: -50 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ duration: 1, delay: 0.2, ease: [0.16, 1, 0.3, 1] }}
-            className="font-nove text-white text-5xl md:text-6xl lg:text-[5.5rem] leading-[1] tracking-tighter mb-8 pr-4"
+            className="text-white lg:text-[5.5rem] leading-[1] tracking-tighter mb-8 pr-4 w-1/2"
            >
              {settings.title}
            </motion.h1>
--- a/src/components/homepage/OurStorySection.tsx
+++ b/src/components/homepage/OurStorySection.tsx
@@ -25,7 +25,7 @@ export function OurStorySection({ settings }: OurStorySectionProps) {
             whileInView={{ opacity: 1, y: 0 }}
             viewport={{ once: true }}
             transition={{ duration: 1, ease: [0.16, 1, 0.3, 1] }}
-            className="text-5xl md:text-6xl lg:text-8xl font-argine font-medium leading-[0.9] tracking-tighter max-w-3xl text-zinc-900"
+            className="font-argine leading-[0.9] tracking-tighter w-full text-zinc-900"
           >
             {settings.title}
           </motion.h2>
--- a/src/components/homepage/SpaWellnessSection.tsx
+++ b/src/components/homepage/SpaWellnessSection.tsx
@@ -12,7 +12,7 @@ interface SpaWellnessSectionProps {
 
 export function SpaWellnessSection({ settings }: SpaWellnessSectionProps) {
   return (
-    <div className="relative min-h-[100dvh] w-full flex flex-col justify-center items-center overflow-hidden py-24 px-6 md:px-12 lg:px-24">
+    <div className="relative min-h-[100dvh] max-h-[1080px] w-full flex flex-col justify-center items-center overflow-hidden py-24 px-6 md:px-12 lg:px-24">
       {/* Background Image with Parallax-like scale */}
       <motion.div 
         initial={{ scale: 1.1 }}
@@ -33,14 +33,14 @@ export function SpaWellnessSection({ settings }: SpaWellnessSectionProps) {
       <div className="absolute inset-0 bg-gradient-to-t from-black/90 via-transparent to-black/90 z-10" />
 
       {/* Centered Content */}
-      <div className="relative z-20 w-full max-w-4xl mx-auto flex flex-col items-center text-center gap-10">
+      <div className="relative z-20 w-full w-full mx-auto flex flex-col items-center text-center gap-10">
         
         <motion.h2 
           initial={{ opacity: 0, y: 50 }}
           whileInView={{ opacity: 1, y: 0 }}
           viewport={{ once: true, margin: "-100px" }}
           transition={{ duration: 1, delay: 0.2, ease: [0.16, 1, 0.3, 1] }}
-          className="font-argine text-white text-5xl md:text-7xl lg:text-[7.5rem] leading-[0.9] tracking-tighter"
+          className="font-argine text-white lg:text-[7.5rem] leading-[0.9] tracking-tighter"
         >
           {settings.title}
         </motion.h2>
@@ -53,7 +53,7 @@ export function SpaWellnessSection({ settings }: SpaWellnessSectionProps) {
           className="flex flex-col items-center gap-10"
         >
           <div 
-            className="text-white/80 max-w-2xl font-light text-xl text-center"
+            className="text-white/80 w-full font-light text-xl text-center"
             dangerouslySetInnerHTML={{ __html: settings.description }}
           />
           
--- a/src/components/homepage/TestimonialsSection.tsx
+++ b/src/components/homepage/TestimonialsSection.tsx
@@ -22,7 +22,7 @@ export function TestimonialsSection({ settings }: TestimonialsSectionProps) {
           transition={{ duration: 0.8 }}
           className="text-center mb-16"
         >
-          <h2 className="text-4xl md:text-5xl font-argine font-normal text-white mb-6">
+          <h2 className="text-white mb-6">
             {settings.title}
           </h2>
         </motion.div>
@@ -55,8 +55,8 @@ export function TestimonialsSection({ settings }: TestimonialsSectionProps) {
                   {item.name.charAt(0)}
                 </div>
                 <div>
-                  <h4 className="font-bold text-white tracking-wide text-base">{item.name}</h4>
-                  <p className="text-sm text-zinc-500 uppercase tracking-wider font-medium mt-1">{item.role}</p>
+                  <h4 className="text-white tracking-wide">{item.name}</h4>
+                  <p className="text-zinc-500 uppercase tracking-wider mt-1 text-justify tracking-tight">{item.role}</p>
                 </div>
               </div>
             </motion.div>
--- a/src/components/homepage/types.ts
+++ b/src/components/homepage/types.ts
@@ -4,6 +4,8 @@ export interface HomepageSettings {
     secondaryColor: string;
     fontFamily: string;
     headingFontFamily: string;
+    headingFontSize?: string;
+    bodyFontSize?: string;
   };
   loader: {
     isVisible: boolean;
@@ -14,6 +16,7 @@ export interface HomepageSettings {
     title: string;
     subtitle: string;
     videoUrl: string;
+    image?: string;
     backgroundImageFallback: string;
     buttonLabel: string;
     titleSize: string;
@@ -105,7 +108,9 @@ export const defaultHomepageSettings: HomepageSettings = {
     primaryColor: 'bg-zinc-900',
     secondaryColor: 'bg-zinc-100',
     fontFamily: 'font-sans',
-    headingFontFamily: 'font-serif',
+    headingFontFamily: 'font-argine',
+    headingFontSize: '1rem',
+    bodyFontSize: '16px',
   },
   loader: {
     isVisible: true,
@@ -116,6 +121,7 @@ export const defaultHomepageSettings: HomepageSettings = {
     title: 'Where Elegance Meets Exceptional Service',
     subtitle: 'Step into a world of refined beauty and uncompromising comfort. Your extraordinary journey begins the moment you arrive.',
     videoUrl: 'https://videos.pexels.com/video-files/853889/853889-hd_1920_1080_25fps.mp4',
+    image: '',
     backgroundImageFallback: 'https://images.unsplash.com/photo-1542314831-c53cd4b85d85?q=80&w=3270&auto=format&fit=crop',
     buttonLabel: 'Reserve Your Experience',
     titleSize: 'text-6xl md:text-8xl leading-tight',
